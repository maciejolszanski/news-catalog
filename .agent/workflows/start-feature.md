---
description: Start a new feature by syncing with master and creating a fresh branch.
---

// turbo-all
1. Checkout to the base branch: `git checkout master`.
2. Pull the latest changes from the remote: `git pull origin master`.
3. Create and switch to a new feature branch: `git checkout -b feature/[feature-name]`.
